import 'package:flutter/material.dart';

class StartPostingAd extends StatelessWidget {
  const StartPostingAd({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
